---
import type { SanityDocument } from "@sanity/client";
import { sanityClient } from "sanity:client";
import Base from "../../layouts/Base.astro";
import { Image } from "astro:assets";
import { urlFor } from "../../scripts/helpers";
import ContactForm from "../../components/ContactForm.astro";
import Title from "../../components/Title.astro";
import Socials from "../../components/Socials.astro";

const QUERY = `*[
  _type == "about"
]|{_id, email, address, instagram, facebook, contactImage{
    asset->{
      _id,
      url,
      metadata{
        dimensions
      }
    }
  }}`;

const about = await sanityClient.fetch<SanityDocument[]>(QUERY);
---

<Base title="Contact">
    <Title content="CONTACT ME" />
    <section class="container">
        <Image
            src={urlFor(about[0].contactImage.asset.url)
                .width(800)
                .format("webp")
                .url()}
            width={800}
            height={Math.round(
                (about[0].contactImage.asset.metadata.dimensions.height /
                    about[0].contactImage.asset.metadata.dimensions.width) *
                    800,
            )}
            alt="About me image"
            class="image"
        />
        <div class="body">
            <p>You can drop me a line at:</p>
            <a href={`mailto:${about[0].email}`}>
                {about[0].email.split("@")[0]}<b
                    >@{about[0].email.split("@")[1]}</b
                >
            </a>
            <div>You can also follow me on socials:</div>
            <Socials
                instagram={about[0].instagram}
                facebook={about[0].facebook}
                linkedin={about[0].linkedin}
                behance={about[0].behance}
                phone={about[0].phone}
            />
            <p>Or you can fill the form below:</p>
            <ContactForm />
        </div>
    </section>
</Base>

<style>
    .container {
        width: 100%;
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 2.5rem;
        align-items: start;
        margin-top: 2.5rem;
    }

    .image {
        width: 100%;
        height: auto;
    }

    .body {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        line-height: 1.5;
    }

    @media screen and (max-width: 1000px) {
        .container {
            grid-template-columns: 1fr;
            gap: 1rem;
        }
    }
</style>
