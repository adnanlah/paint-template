<!doctype html>
<html lang="en">
	<head>
		<!-- Google tag (gtag.js) -->
		<script
			async
			src="https://www.googletagmanager.com/gtag/js?id=G-JBFSPQRVTD"
		></script>
		<script>
			window.dataLayer = window.dataLayer || [];
			function gtag() {
				dataLayer.push(arguments);
			}
			gtag("js", new Date());

			gtag("config", "G-JBFSPQRVTD");
		</script>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>El Bahi BELKHEIRI Portfolio</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	@import url("https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&family=Playfair:ital,opsz,wght@0,5..1200,300..900;1,5..1200,300..900&family=Raleway:ital,wght@0,100..900;1,100..900&display=swap");

	html,
	body {
		font-family: "Raleway", sans-serif;
		font-optical-sizing: auto;
		min-height: 100vh;
		font-size: 14px;
		font-weight: 400;
		margin: 0;
		padding: 0;
		width: 100%;
		background: #fcfffc;
		overflow-x: hidden;
	}

	a,
	a:visited,
	a:active {
		color: inherit;
		text-decoration: none;
		position: relative;
	}

	/* a.styledLink::after {
		content: "";
		position: absolute;
		left: 0;
		bottom: -3px;
		width: 0;
		height: 2.5px;
		background-color: currentColor;
		transition: width 0.3s ease;
	}

	a.styledLink.active::after {
		width: 100%;
	}

	a.styledLink:hover::after {
		width: 100%;
	} */

	a.styledLink {
		/* transition: all 0.1s ease-in-out; */
	}

	a.styledLink.active,
	a.styledLink:hover {
		color: #000 !important;
		font-weight: 600;
	}

	/* a.styledLink:hover {
		color: #000;
		font-weight: 600;
	} */

	a.styledImage {
		display: block;
		overflow: hidden;
		border-radius: inherit;
		/* Needed for iOS safari clipping */
		-webkit-mask-image: -webkit-radial-gradient(white, black);
	}

	a.styledImage img {
		display: block;
		width: 100%;
		height: auto;
		overflow: hidden !important;
		transition: transform 0.3s ease-in-out;
	}

	a.styledImage:hover img {
		transform: scale(1.02);
	}

	h1,
	h2,
	h3,
	h4,
	h5,
	h6 {
		margin: 0;
		padding: 0;
	}

	p {
		margin: 0;
		padding: 0;
	}

	.red {
		color: red;
	}

	.clickableImage {
		transition: transform 0.3s ease-in-out;
	}

	.clickableImage.left:hover {
		transform: translateX(-0.5rem);
	}

	.clickableImage.right:hover {
		transform: translateX(0.5rem);
	}
</style>
